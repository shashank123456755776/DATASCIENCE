# Anonymous Function --without name
# lambda arguments:expression condition 
# only one expression can be send
add_Ten=lambda x:x*2
print(add_Ten(5))
# map --for iterations 
def square(x):
    return x*2
nums=[1,2,3,4,5]
result=map(square,nums)
print(list(result))
for  i in nums:
    lambda i:i*2
    print(i)

